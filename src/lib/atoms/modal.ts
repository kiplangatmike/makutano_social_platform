import { atom } from "jotai";

export const modalState = atom(false);

export const modalStateAddChapters = atom(false);

export const postForChapters = atom("");

export const editingPost = atom(null);

export const editingChapter = atom(null);

export const modalTypeState = atom("dropIn");

export const modalState2 = atom(false);

export const modalTypeState2 = atom("dropIn");
